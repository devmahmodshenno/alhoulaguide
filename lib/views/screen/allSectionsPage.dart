import 'package:flutter/material.dart';

class Allsectionspage extends StatelessWidget {
  const Allsectionspage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('All Sections Page'),
    );
  }
}